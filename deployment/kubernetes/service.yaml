apiVersion: v1
kind: Service
metadata:
  name: infinity-ai-service
  namespace: infinity
spec:
  selector:
    app: infinity-ai
  ports:
    - name: http
      port: 8000
      targetPort: 8000
    - name: metrics
      port: 8001
      targetPort: 8001
  type: ClusterIP
